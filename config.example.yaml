opus_id: "<opus静态ID>"
up_uid: "<UP主的mid>"
poll_interval: 180
history_days: 1
push_interval: 2
fast_interval: 60
slow_interval: 300

bilibili:
  sessdata: "<从浏览器F12->Application->Cookies->bilibili.com复制SESSDATA>"

feishu:
  webhook: "https://open.feishu.cn/open-apis/bot/v2/hook/example-token"
  secret: "example-signing-secret"  # 开启“签名校验”时填写；未开启可删除
